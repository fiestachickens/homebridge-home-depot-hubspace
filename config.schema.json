{
  "pluginAlias": "HomeDepotHubspace",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "HomeDepotHubspace"
      },
      "username": {
        "title": "Username",
        "type": "string",
        "required": true,
        "description": "Your Hubspace username",
        "placeholder": "Username"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "Your Hubspace password",
        "placeholder": "Password"
      },
      "loginBuffer": {
        "title": "Login Buffer Time",
        "type": "integer",
        "required": true,
        "description": "Buffer time before token expiration (in seconds). Default is 300 seconds.",
        "default": 300
      }
    }
  }
}
